meta {
  name: Delete Area User
  type: http
  seq: 21
}

http {
  method: DELETE
  url: {{host}}/api/users/3
}

tests {
  test("Status is 204", function() {
    expect(res.getStatus()).to.equal(204);
  });
}
