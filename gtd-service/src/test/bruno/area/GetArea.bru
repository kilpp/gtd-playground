meta {
  name: Get Area
  type: http
  seq: 15
}

get {
  url: {{host}}/api/areas/{{areaId}}
}

headers {
  Content-Type: application/json
}

tests {
  test("Status is 200", function() {
    expect(res.getStatus()).to.equal(200);
  });
  test("Response has name", function() {
    const data = res.getBody();
    expect(data.name).to.be.a("string");
  });
}
