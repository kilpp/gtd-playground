meta {
  name: Delete Context User
  type: http
  seq: 20
}

http {
  method: DELETE
  url: {{host}}/api/users/2
}

tests {
  test("Status is 204", function() {
    expect(res.getStatus()).to.equal(204);
  });
}
