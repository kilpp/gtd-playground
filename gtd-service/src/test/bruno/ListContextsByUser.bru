meta {
  name: List Contexts by User
  type: http
  seq: 9
}

http {
  method: GET
  url: {{host}}/api/contexts?userId=2
}

tests {
  test("Status is 200", function() {
    expect(res.getStatus()).to.equal(200);
  });
  test("Response is array with 1 item", function() {
    const data = res.getBody();
    expect(data).to.be.an('array');
    expect(data.length).to.equal(1);
    expect(data[0].name).to.equal("@Home");
  });
}
