http {
  method: PUT
  url: {{host}}/api/contexts/1
}

headers {
  Content-Type: application/json
}

body:json {
    "userId": 2,
    "name": "@Home Updated",
    "description": "Updated",
    "isLocation": false
}

tests {
  test("Status is 200", function() {
    expect(res.getStatus()).to.equal(200);
  });
  test("Response has updated name", function() {
    const data = res.getBody();
    expect(data.name).to.equal("@Home Updated");
  });
}
