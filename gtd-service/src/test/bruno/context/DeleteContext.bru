meta {
  name: Delete Context
  type: http
  seq: 11
}

delete {
  url: {{host}}/api/contexts/{{contextId}}
}

tests {
  test("Status is 204", function() {
    expect(res.getStatus()).to.equal(204);
  });
}

script:post-response  {
  // Delete a user used for context tests
  const response = await bru.runRequest("user/DeleteUser.bru");
}