meta {
  name: Get Context
  type: http
  seq: 9
}

get {
  url: {{host}}/api/contexts/{{contextId}}
}

tests {
  test("Status is 200", function() {
    expect(res.getStatus()).to.equal(200);
  });
  test("Response has name", function() {
    const contextName = bru.getVar("contextName");
    const data = res.getBody();
    expect(data.name).to.equal(contextName);
  });
}
