meta {
  name: Update Tag
  type: http
  seq: 5
}

put {
  url: {{host}}/api/tags/{{tagId}}
}

headers {
  Content-Type: application/json
}

body {
    {
      "userId": "{{userId}}",
      "name": "UpdatedTag"
    }
}

tests {
  test("Status is 200", function() {
    expect(res.getStatus()).to.equal(200);
  });
  test("Response has updated name", function() {
    const data = res.getBody();
    expect(data.name).to.equal("UpdatedTag");
    bru.setVar("tagName", data.name);
  });
}
