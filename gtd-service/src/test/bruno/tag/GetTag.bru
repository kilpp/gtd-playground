meta {
  name: Get Tag
  type: http
  seq: 3
}

get {
  url: {{host}}/api/tags/{{tagId}}
}

tests {
  test("Status is 200", function() {
    expect(res.getStatus()).to.equal(200);
  });
  test("Response has name", function() {
    const tagName = bru.getVar("tagName");
    const data = res.getBody();
    expect(data.name).to.equal(tagName);
  });
}
