meta {
  name: Delete Area
  type: http
  seq: 19
}

http {
  method: DELETE
  url: {{host}}/api/areas/1
}

tests {
  test("Status is 204", function() {
    expect(res.getStatus()).to.equal(204);
  });
}
