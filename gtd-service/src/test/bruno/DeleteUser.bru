meta {
  name: Delete User
  type: http
  seq: 5
}

http {
  method: DELETE
  url: {{host}}/api/users/1
}

tests {
  test("Status is 204", function() {
    expect(res.getStatus()).to.equal(204);
  });
}
