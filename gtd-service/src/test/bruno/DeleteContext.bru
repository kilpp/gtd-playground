meta {
  name: Delete Context
  type: http
  seq: 12
}

http {
  method: DELETE
  url: {{host}}/api/contexts/1
}

tests {
  test("Status is 204", function() {
    expect(res.getStatus()).to.equal(204);
  });
}
