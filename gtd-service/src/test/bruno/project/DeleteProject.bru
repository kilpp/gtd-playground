meta {
  name: Delete Project
  type: http
  seq: 28
}


delete {
  url: {{host}}/api/projects/{{projectId}}
}

headers {
  Content-Type: application/json
}

tests {
  test("Status is 204", function() {
    expect(res.getStatus()).to.equal(204);
  });
}
 
script:post-response  {
  // Delete the area used for project tests
  const areaResponse = await bru.runRequest("area/DeleteArea.bru");
  
}
