meta {
  name: Get User
  type: http
  seq: 3
}

http {
  method: GET
  url: {{host}}/api/users/1
}

tests {
  test("Status is 200", function() {
    expect(res.getStatus()).to.equal(200);
  });
  test("Response has username", function() {
    const data = res.getBody();
    expect(data.username).to.equal("bruno-user");
  });
}
